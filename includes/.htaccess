# Block direct HTTP access to server-side includes.
# PHP can still include these files from disk; only web requests are denied.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
